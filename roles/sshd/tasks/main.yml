---
- name: install sshd configuration
  notify: "restart sshd"
  copy:
    src: "sshd_config"
    dest: "/etc/ssh/sshd_config"
    validate: "sshd -tf %s"
- name: enable sshd
  service:
    name: sshd
    enabled: true
    state: started
